import { Layout } from "antd";
const { Footer } = Layout;
const App = () => {
    return (
        <Layout className="min-h-max flex flex-col absolute bottom-0 w-full">
            <Footer className="text-center">By @ Thiều Trần Cương</Footer>
        </Layout>
    );
};
export default App;
